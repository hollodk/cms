{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}
    <h1>Site</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ site.title }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ site.createdAt ? site.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ site.updatedAt ? site.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mh_page_site_index') }}">back to list</a>

    <a href="{{ path('mh_page_site_edit', {'id': site.id}) }}">edit</a>

    {{ include('@MhPage/site/_delete_form.html.twig') }}
{% endblock %}
