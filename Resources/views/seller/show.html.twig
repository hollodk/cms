{% extends 'base.html.twig' %}

{% block title %}Seller{% endblock %}

{% block body %}
    <h1>Seller</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seller.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ seller.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ seller.createdAt ? seller.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ seller.updatedAt ? seller.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mh_page_seller_index') }}">back to list</a>

    <a href="{{ path('mh_page_seller_edit', {'id': seller.id}) }}">edit</a>

    {{ include('@MhPage/seller/_delete_form.html.twig') }}
{% endblock %}
