{% extends 'base.html.twig' %}

{% block title %}Edit Referral{% endblock %}

{% block body %}
    <h1>Edit Referral</h1>

    {{ include('referral/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('referral_index') }}">back to list</a>

    {{ include('referral/_delete_form.html.twig') }}
{% endblock %}
